apiVersion: v1
kind: ConfigMap

metadata:
  name: notes-config

data:
  DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres-service:5432/notesdb
  ALGORITHM: HS256
  ACCESS_TOKEN_EXPIRE_MINUTES: "60"